process {
    withName: IRODS_GETMETADATA {
        maxForks = 50
        array    = 1000
        queue    = { task.attempt == 1 ? 'small' : 'normal' }
        memory   = { 90.MB + 1000.MB * (task.attempt - 1) }
        cpus     = 1
    }
}
