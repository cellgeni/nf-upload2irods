process {
    withName: CSV_CONCAT {
        ext.args = {
            [
                "--axis 'index'",
                "--join 'outer'",
            ].join(' ')
        }
        queue    = 'normal'
        cpus     = 1
        memory   = '2 GB'
    }
}
